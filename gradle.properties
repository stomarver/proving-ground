# Do not hardcode org.gradle.java.home here: it is machine-specific.
# Set Gradle JVM in IntelliJ (recommended JDK 24 for sync stability),
# while project compilation stays on Java 25 via toolchain in build.gradle.
org.gradle.java.installations.auto-detect=true
org.gradle.warning.mode=all
